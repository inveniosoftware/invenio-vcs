# SPDX-FileCopyrightText: 2023-2026 CERN.
# SPDX-FileCopyrightText: 2023-2026 Graz University of Technology.
# SPDX-FileCopyrightText: 2023 Graz University of Technology.
# SPDX-FileCopyrightText: 2025-2026 CERN.
# SPDX-FileCopyrightText: 2026 TU Wien.
# SPDX-License-Identifier: MIT

[project]
name = "invenio-vcs"
description = "Invenio module that adds VCS integration to the platform."
readme = "README.rst"
requires-python = ">= 3.9"
license = "MIT"
authors = [
  { name = "CERN", email = "info@inveniosoftware.org" },
]
keywords = [
  "github",
  "gitlab",
  "invenio",
  "vcs",
]
classifiers = [
  "Development Status :: 5 - Production/Stable",
]
dependencies = [
  "click>=8.1.8",
  "email-validator>=1.0.5",
  "flask-menu>=2.0.0,<3.0.0",
  "github3.py>=4.0.1,<5.0.0",
  "humanize>=0.5.1",
  "invenio-accounts>=9.0.0,<10.0.0",
  "invenio-assets>=4.0.0,<5.0.0",
  "invenio-celery>=2.0.0,<3.0.0",
  "invenio-db[postgresql,mysql]>=2.0.0,<3.0.0",
  "invenio-formatter>=5.0.0,<6.0.0",
  "invenio-i18n>=4.0.0,<5.0.0",
  "invenio-jobs>=11.0.0,<12.0.0",
  "invenio-notifications>=2.0.0,<3.0.0",
  "invenio-oauth2server>=6.0.0,<7.0.0",
  "invenio-oauthclient>=9.0.0,<10.0.0",
  "invenio-pidstore>=4.0.0,<5.0.0",
  "invenio-records-resources>=11.0.0,<12.0.0",
  "invenio-records-rest>=6.0.0,<7.0.0",
  "invenio-users-resources>=13.0.0,<14.0.0",
  "invenio-webhooks>=4.0.0,<5.0.0",
  "mistune>=0.7.2",
  "python-dateutil>=2.9.0,<3.0.0",
  "python-gitlab>=6.2.0,<7.0.0",
  "pyyaml>=5.4.1",
  "six>=1.12.0",
  "uritemplate>=3.0.1",
]
dynamic = ["version"]

[project.urls]
Homepage = "https://github.com/inveniosoftware/invenio-vcs"

[project.entry-points."invenio_assets.webpack"]
invenio_vcs = "invenio_vcs.webpack:theme"

[project.entry-points."invenio_base.api_apps"]
invenio_vcs = "invenio_vcs:InvenioVCS"

[project.entry-points."invenio_base.api_blueprints"]
invenio_vcs = "invenio_vcs.views.vcs:create_api_blueprint"

[project.entry-points."invenio_base.api_finalize_app"]
invenio_vcs = "invenio_vcs.ext:finalize_app_api"

[project.entry-points."invenio_base.apps"]
invenio_vcs = "invenio_vcs:InvenioVCS"

[project.entry-points."invenio_base.blueprints"]
invenio_vcs_badge = "invenio_vcs.views.badge:blueprint"
invenio_vcs_vcs = "invenio_vcs.views.vcs:create_ui_blueprint"

[project.entry-points."invenio_base.finalize_app"]
invenio_vcs = "invenio_vcs.ext:finalize_app_ui"

[project.entry-points."invenio_celery.tasks"]
invenio_vcs = "invenio_vcs.tasks"

[project.entry-points."invenio_db.alembic"]
invenio_vcs = "invenio_vcs:alembic"

[project.entry-points."invenio_db.models"]
invenio_vcs = "invenio_vcs.models"

[project.entry-points."invenio_i18n.translations"]
messages = "invenio_vcs"

[project.entry-points."invenio_jobs.jobs"]
refresh_vcs_accounts = "invenio_vcs.jobs:RefreshAccountsJob"

[project.optional-dependencies]
elasticsearch7 = [
  "invenio-search[elasticsearch7]>=3.0.0,<4.0.0",
]
opensearch1 = [
  "invenio-search[opensearch1]>=3.0.0,<4.0.0",
]
opensearch2 = [
  "invenio-search[opensearch2]>=3.0.0,<4.0.0",
]
tests = [
  "httpretty>=0.8.14",
  "invenio-app>=3.0.0,<4.0.0",
  "invenio-db[postgresql,mysql]>=2.0.0,<3.0.0",
  "invenio-files-rest>=6.0.0,<7.0.0",
  "isort>=4.2.2",
  "pytest-black>=0.6.0",
  "pytest-invenio>=3.0.0,<4.0.0",
  "pytest-mock>=2.0.0",
  "sphinx>=4.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# see https://babel.pocoo.org/en/latest/messages.html#toml-configuration-format
[tool.babel]
[tool.babel.compile_catalog]
directory = "invenio_vcs/translations/"
use_fuzzy = true

[tool.babel.extract_messages]
copyright_holder = "CERN"
msgid_bugs_address = "info@inveniosoftware.org"
mapping_file = "pyproject.toml"
output_file = "invenio_vcs/translations/messages.pot"
add_comments = "NOTE"

[tool.babel.init_catalog]
input_file = "invenio_vcs/translations/messages.pot"
output_dir = "invenio_vcs/translations/"

[[tool.babel.mappings]]
method = "javascript"
pattern = "**.js"
encoding = "utf-8"
extract_messages = "$._, jQuery._"

[[tool.babel.mappings]]
method = "python"
pattern = "**.py"
encoding = "utf-8"

[[tool.babel.mappings]]
method = "jinja2"
pattern = "**/templates/**.html"
encoding = "utf-8"

[tool.hatch.version]
path = "invenio_vcs/__init__.py"

[tool.hatch.build.targets.sdist]
include = [
  "/invenio_vcs",
]

[tool.isort]
profile = "black"

[tool.pydocstyle]
add_ignore = "D401,D403"

[tool.pytest.ini_options]
addopts = '--black --isort --pydocstyle --ignore=docs --cov=invenio_vcs --cov-report=term-missing'
testpaths = "tests invenio_vcs"
live_server_scope = "module"

# UV needs to be told explicitly which extras are conflicting:
# https://docs.astral.sh/uv/concepts/projects/config/#conflicting-dependencies
[tool.uv]
conflicts = [
  [
    { extra = "opensearch1" },
    { extra = "opensearch2" },
    { extra = "elasticsearch7" },
  ]
]
